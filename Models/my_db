const mongoose = require('mongoose');

const schema = new mongoose.Schema({
    id:Number,
    name:String,
    genre:String,
    actors:[String]
})

module.exports = mongoose.model('sinners',schema);
